package chapter4;

public interface Gorilla {
	
	// In an interface all access modifiers are public and could not be specified
	// Also, if not specified the method is abstract (= so without a body)
	String move();
	
	// An interface can have static methods
	// An interface can have default methods; they must have bodies.

}
